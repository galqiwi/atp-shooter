#pragma once

#include <iostream>
#include <vector>
#include <memory>
#include <fstream>
#include <string>
#include <sstream>
#include <random>
#include "math.h"

const double FIREBALL_SIZE = 1.0;
const size_t FIREBALL_TEXTURE = 0;
const float ENEMIES_PER_CELL = 0.5;
const float ENEMY_PIXEL_SIZE = 0.1;
const size_t ENEMY_DEFAULT_HEALTH = 100;